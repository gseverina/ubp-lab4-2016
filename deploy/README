# Building the docker image:
docker build -t gseverina/ansible-tutorial .

# Pushing the image to the registry:
docker push gseverina/ansible-tutorial

# Running the image:
docker run -v /home/gseverina/repos/ubp-lab4-2016/deploy/playbooks:/usr/playbooks -ti gseverina/ansible-tutorial sh
